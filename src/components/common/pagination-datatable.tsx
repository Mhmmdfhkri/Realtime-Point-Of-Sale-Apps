import { Pagination, PaginationContent } from "../ui/pagination";

export default function PaginationDatatable() {
    return (
        <Pagination >
            <PaginationContent>
                
            </PaginationContent>
        </Pagination>
    )
}